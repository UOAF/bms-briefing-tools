interface:
  display_name: "BMS Briefing Planner"
  short_description: "Falcon BMS briefs, maps, weather, and optional 3D target views"
  default_prompt: "Use $bms-briefing-planner to create a Falcon BMS mission brief, slide-ready maps, and optional 3D target imagery from my campaign save, package IDs, and planner intent."

policy:
  allow_implicit_invocation: true
